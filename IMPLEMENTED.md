# Shahdab — Implementation Tracker

> Update this file after every meaningful edit so the current state is always visible.

## Progress

| Section | Status | Key files / notes |
|---|---|---|
| 1. Project setup & architecture | Done | `frontend/` bootstrapped with Vite + React 18 + TypeScript + Tailwind 3.4 + shadcn/ui-style theming. `src/lib/utils.ts`, `src/components/ui/button.tsx`, `src/services/events.ts`, `src/services/mockApi.ts`, `src/mocks/db.ts`, `src/store/useAppStore.ts`. `npm run build` passes. |
| 2. Data models & mock data | Done | `src/types/index.ts`, `src/mocks/db.ts`, `src/services/mockApi.ts`. All schema-derived types, seeded demo org (2 outlets, 2 floors, 3 kitchens, 8 tables, 15 menu items, 6 staff, active/reserved orders, bills, taxes, notifications, audit logs), `DataService` CRUD, role/permission matrix, and `mockApi` routing. `npm run build` passes. |
| 3. Authentication & role management | Done | `src/contexts/AuthContext.tsx` with `useAuth`, `src/pages/Login.tsx`, `src/pages/NotificationsPage.tsx`, `src/components/PermissionGuard.tsx`, `src/components/Forbidden.tsx`. Role-aware login, outlet switching, permission checks, unread notification badge, mark-as-read, and sign-out. `npm run build` passes. |
| 4. Routing & access control | Done | `src/app/routes.tsx` route map, `src/app/nav.ts` filtered nav, `src/components/PrivateRoute.tsx`, `src/components/PermissionRoute.tsx`, `src/components/AppShell.tsx` (side navigation layout), `src/components/NotFound.tsx`, `src/pages/PlaceholderPage.tsx`, default post-login redirects in `Login.tsx`. `npm run build` passes. |
| 5. Pages & features | Done | 5.1 Onboarding & setup: `src/pages/SetupPage.tsx` and `src/pages/StaffPage.tsx`. 5.2 Reservations: `src/pages/ReservationsPage.tsx`. 5.3 Dine-in orders — complete: `src/pages/TablesPage.tsx` plus `src/components/MenuBrowser.tsx` and `src/components/TableOperations.tsx`. 5.4 Kitchen & stock — complete: `src/pages/KitchenPage.tsx` (dine-in ticket board), `src/pages/KitchenTakeawayPage.tsx` (takeaway queue + per-item `Accept`/`Start Cooking` + Mark All Ready), `src/pages/StockOutPage.tsx`. 5.5 Takeaway — complete: `src/pages/TakeawayPage.tsx` (customer form, menu/cart, `order_type = takeaway` creation, active order list, `Bill & Pick Up` flow with tax, payment, and order close). 5.6 Billing — complete: `src/pages/BillingPage.tsx` (queue, bill builder, item/bill discounts, by-number/by-item splits, payments, close), `src/pages/BillHistoryPage.tsx` (paid bill list), `src/pages/BillDetailPage.tsx` (receipt with print). 5.7 Waiter actions — complete: `src/pages/TablesPage.tsx` (My Tables view for waiters, mark items served, request bill from table). 5.8 Analytics — complete: `src/pages/AnalyticsPage.tsx` (date range + granularity filters, revenue trend, payment split, best/worst dishes, category performance, staff performance, kitchen efficiency, table turnover, peak hours, voids & discounts) and `src/services/analytics.ts` aggregation helpers. 5.9 Audit — complete: `src/pages/AuditLogsPage.tsx` (filters: entity type, staff, date range; before/after diff drawer) and `src/services/audit.ts` helper; logging wired into `StockOutPage.tsx`, `TableOperations.tsx` (merge/split/transfer), `TablesPage.tsx` (item cancel), and `BillingPage.tsx` (discount). Wired in `src/app/routes.tsx`. `npm run build` passes. |
| 6. Reusable UI components | Done | `src/components/StatusBadge.tsx`, `src/components/TableCard.tsx`, `src/components/OrderTicket.tsx`, `src/components/MenuItemCard.tsx`, `src/components/CartSummary.tsx`, `src/components/BillBuilder.tsx`, `src/components/NotificationBell.tsx`, `src/components/NotificationToast.tsx`, `src/components/DataTable.tsx`, `src/components/AnalyticsChart.tsx`, `src/components/AuditDiff.tsx` (plus existing `src/components/Forbidden.tsx` and `src/components/NotFound.tsx`). `npm run build` passes. |
| 7. Validation & error/empty/loading states | Done | `src/lib/validation.ts` (Zod schemas for org, outlet, staff, reservation, order, bill, payment), `src/lib/apiErrors.ts` (typed error code map), `src/services/mockApi.ts` (API-style error envelope for business actions: merge/split/transfer, cancel item), `src/components/ui/skeleton.tsx`, `src/components/LoadingState.tsx`, `src/components/EmptyState.tsx`, `src/components/DataTable.tsx` (skeleton + empty state), `src/pages/SetupPage.tsx`, `src/pages/StaffPage.tsx`, `src/pages/ReservationsPage.tsx`, `src/pages/TakeawayPage.tsx`, `src/pages/BillingPage.tsx` (Zod form validation), `src/components/TableOperations.tsx`, `src/pages/TablesPage.tsx` (use `mockApi` for API-style errors). `npm run build` passes. |
| 8. Testing | Done | Vitest + jsdom + Testing Library installed. `npm run test` script added. `vitest.config.ts`, `src/test/setup.ts`, and tests for state machines (`src/lib/stateMachines.test.ts`), DataService/permissions (`src/mocks/db.test.ts`), `mockApi` (`src/services/mockApi.test.ts`), validation (`src/lib/validation.test.ts`), Login/RBAC redirects (`src/pages/Login.test.tsx`), `StatusBadge` and `BillBuilder`. 29 tests pass; `npm run build` passes. |
| 9. Final polish & verification | Done | `README.md` with run instructions and mock credential cheat sheet, final `tsc` + `npm run build` + `npm test` pass, `StatusBadge` `role="status"` + `aria-label`, `AppShell` outlet `aria-label` and responsive `main` padding, global `focus-visible` outline. Final review against `docs/workflow.md`, `docs/api_design_doc.md`, and `docs/db_schema.md` completed; all sections are represented in code or documented as out-of-scope in `README.md`. |
| 10. UI/UX design implementation guide | Done | `docs/DESIGN_IMPLEMENTATION.md` created. Defines global design tokens, status color map, shared component contract, and per-page layout specs for parallel subagent redesign. No code changes yet. |

|| 11. Page UI redesign pass | Done | Applied global tokens and `print` utilities; launched 6 parallel subagents. All completed: shared UI, auth/notifications, billing, admin/analytics/audit, kitchen/stock, tables/reservations/takeaway. `npx tsc --noEmit` and `npm run build` pass. |

||| 12. QR waitlist / customer queue | Done | Added `WaitlistEntry` type and `waitlist` collection, table occupancy timing fields (`avg_time_per_person`, `occupied_at`, `occupied_by_count`, `expected_vacant_at`), `src/services/waitlist.ts` helpers (`addToWaitlist`, `estimatedWaitMinutes`, `notifyNextCustomer`, `seatWaitlistEntry`, `markTableVacant`), public customer `src/pages/CustomerQueuePage.tsx` at `/queue/:outletId` with live wait estimate and WhatsApp-ready notification, staff `src/pages/WaitlistPage.tsx` at `/waitlist` for seat/no-show/remove, `Mark Clean` in `src/pages/TablesPage.tsx` to notify the next customer, and wiring in `ReservationsPage.tsx`, `MenuBrowser.tsx`, `SetupPage.tsx`, `src/app/routes.tsx`, and `src/app/nav.ts`. `npm run build` and `npm run test` pass. |

||||| 13. Menu management page | Done | `src/pages/MenuPage.tsx` with add/edit form for menu items, category and kitchen selection, default variant base price plus per-floor price overrides, modifier toggles, active/inactive flag, and delete; wired into `src/app/routes.tsx` at `/menu` replacing the placeholder. `npm run build` passes. ||

## Last updated

2026-08-31 — Added dedicated `/menu` management page (`MenuPage`) for listing, adding, editing, and removing menu items. `npm run build` passes.
